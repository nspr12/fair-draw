<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fair.draw.mapper.EventPrizeMapper">

    <select id="findByEvent" resultType="com.fair.draw.domain.EventPrize">
        SELECT id, event_id, rank_type, prize_name, prize_count
        FROM event_prize
        WHERE event_id = #{eventId}
        ORDER BY rank_type ASC
    </select>

    <select id="findById" resultType="com.fair.draw.domain.EventPrize">
        SELECT id, event_id, rank_type, prize_name, prize_count
        FROM event_prize
        WHERE id = #{id}
    </select>
</mapper>