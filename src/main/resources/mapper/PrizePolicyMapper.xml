<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fair.draw.mapper.PrizePolicyMapper">

    <select id="findByEvent" resultType="com.fair.draw.domain.EventPrize">
        SELECT id, event_id, rank_type, prize_count, match_count,
               start_no, end_no, description
        FROM prize_policy
        WHERE event_id = #{eventId}
        ORDER BY rank_type
    </select>

</mapper>