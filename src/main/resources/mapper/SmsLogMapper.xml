<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lotto.event.mapper.SmsLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sms_log (participant_id, phone_number, message_type, content, send_status)
        VALUES (#{participantId}, #{phoneNumber}, #{messageType}, #{content}, #{sendStatus})
    </insert>

    <select id="countByEvent" resultType="int">
        SELECT COUNT(*) FROM sms_log sl
                                 JOIN participant p ON sl.participant_id = p.id
        WHERE p.event_id = #{eventId}
    </select>

    <delete id="deleteByEvent">
        DELETE FROM sms_log WHERE participant_id IN
                                  (SELECT id FROM participant WHERE event_id = #{eventId})
    </delete>
</mapper>